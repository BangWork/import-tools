import { t } from 'i18next';

export const LOCAL_CONFIG = {
  itemsUnit: t('importProject.local.itemUnit'),
  itemUnit: t('importProject.local.itemUnit'),
  searchPlaceholder: t('importProject.local.searchPlaceholder'),
};

export const listStyle = () => {
  return { width: '50%', height: '100%' };
};
